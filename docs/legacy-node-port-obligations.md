# Legacy Node port obligations

The root Node application is still the only service this repository **ships** — `Dockerfile` and
`.github/workflows/docker-publish.yml` build and run `dist/index.js` — and must not be deleted until
every obligation below has a tested Rust replacement. It is no longer the only service that is
**composed**: `apps/gta-claw-daemon` now depends on 20 `claw-*` crates and binds real listeners over
them. What that composition does and does not reach is stated per row, and no row treats "the daemon
composes it" as "the obligation is discharged". `crates/claw-repo-policy` treats this inventory as a
path ratchet: these exact legacy paths may remain or disappear, but a new JavaScript or TypeScript
path fails the repository policy.

The frozen behavior authority remains `compat/legacy/contract.json` and its ledgers. This document is
the operational deletion checklist and does not relax those contracts.

## Current inventory boundary

The authoritative `origin/main` baseline contains exactly 18 TypeScript files. No `dist/` file,
generated JavaScript output, or unmerged legacy feature path is grandfathered.

PR #39 adds four `src/state/*.ts` modules and four `tests/*.test.mjs` files to the legacy runtime.
Every one of those paths is intentionally outside the inventory and therefore fails this ratchet.
If that feature is still required, it must be implemented in the assigned Rust memory/state crates
or receive an explicit repository-policy decision; it cannot enter silently as legacy expansion.

## Module-by-module obligations

An obligation is discharged only when its behavior is composed end to end in a running service **and
that service is proven against the frozen contract**. A crate that implements the behavior perfectly
discharges nothing on its own, and neither does a composition nobody has tested against
`compat/legacy`. So that a reader can tell what is actually missing from a row, every status names
which of four things is absent:

| Missing piece | Meaning |
|---|---|
| **owner** | No crate in this repository owns the behavior at all. |
| **implementation** | An owner is assigned, but part of the behavior is not written yet. |
| **composition** | An owner implements the behavior with tests, and no running service uses it. |
| **evidence** | An owner implements it and the running service composes it, and no test replays `compat/legacy` against that service. |

The last row is new, and it is where most rows below now sit. `src/index.ts` has a composition:
`apps/gta-claw-daemon/src/production.rs` resolves configuration, binds four listeners, and supervises
every ingress through shutdown, and `apps/gta-claw-daemon/src/main.rs` runs *that* path — not the
older `compose`/`serve` stand-in harness, which is now only an in-process contract test for
`claw-application::composition`. Rows that are still composition failures name the specific part of
the composed service that does not reach them, which is a narrower claim than the blanket "nothing
runs it" this table used to record. It is not a claim that the service is finished: no test in
`apps/` reads `compat/legacy`, so not one obligation below has been discharged.

| Legacy module | Behavior that must be disposed or replaced | Intended Rust replacement | Deletion status |
|---|---|---|---|
| `src/auth/deviceFlow.ts` | GitHub device-code request, reusable instructions, bounded polling, `slow_down`, expiry/denial handling, user lookup, and engine activation. | `claw-providers` + `claw-provider-sdk` + `claw-http-api` | **Composed — evidence outstanding.** `claw-providers::github_copilot` speaks the RFC 8628 grant directly over `claw-provider-sdk`'s `hyper`/`rustls` transport, with the server-dictated interval, `slow_down` backoff, `authorization_pending`, `access_denied`, and `expired_token`. `apps/gta-claw-daemon/src/adapters/legacy.rs::LegacyDeviceFlowAdapter` implements `claw-http-api`'s `LegacyDeviceFlowPort`, is installed in `LegacyApiServices` whenever device flow is enabled, and answers `GET /auth/device` on the bound legacy listener; a completed grant swaps the live provider through `CopilotDeviceActivator`, so the token really does activate the engine. `apps/gta-claw-daemon/tests/production_composition.rs` covers onboarding before provider authentication. Nothing replays `compat/legacy` against it. |
| `src/bot/teamsBot.ts` | Bot Framework activity handling, auth/error behavior, shared engine dispatch, and 4,000-character reply splitting. | `claw-channels` + `claw-http-api` | **Composed — evidence outstanding.** The implementation gap this row used to carry is closed: `LegacyTeamsAdapter::verify_teams_request` requires `RS256`, resolves the signing key from the Bot Framework OpenID configuration and its JWKS with a bounded refresh, pins issuer and audience, requires `exp`/`iss`/`aud`, validates `nbf` with 60 seconds of leeway, requires the `msteams` endorsement on the key, requires `channelId == "msteams"`, and matches the token's `serviceurl` claim to the activity. `claw-channels::teams` still reports `ImplementationStatus::CompatibilityShim` — it emits `TeamsAction` values rather than sending — and the daemon adapter is what turns those into Bot Connector calls over the shared transport. `POST /api/messages` is registered only when Teams is configured. Nothing replays `compat/legacy` against it. |
| `src/channels/discordGateway.ts` | Gateway discovery, identify/heartbeat/reconnect lifecycle, bot filtering, inbound dispatch, REST replies, and error containment. | `claw-channels` | **Composed — evidence outstanding.** `claw-channels::discord` still reports `CompatibilityShim` and owns the gateway phases, identify/heartbeat/reconnect, bot filtering, inbound dispatch, and REST replies. `DiscordTransport` now has an implementation outside the crate's tests: `apps/gta-claw-daemon/src/adapters/channels.rs::DiscordTransportAdapter` carries the WSS socket via `tokio-tungstenite` and the REST replies via `claw-provider-sdk`'s `HttpTransport`, under a `ChannelSupervisor` with a start timeout, a stop grace period, and abandonment accounting. Nothing replays `compat/legacy` against it. |
| `src/channels/messageProcessor.ts` | Shared `/help` handling, unauthenticated Device Flow response, and channel-to-conversation engine dispatch. | `claw-channels` + `claw-runtime` + `claw-provider-sdk` | **Composed — evidence outstanding.** `claw-channels::message_processor` owns the shared command surface, the frozen unconfigured and failure replies, the Device Flow prompt, and dispatch through `ConversationService`. The live channel path calls `dispatch_incoming` with `AuthenticationPrompt::Unconfigured` and `AuthenticationPrompt::Instructions`, and `RuntimeConversation` in `apps/gta-claw-daemon/src/adapters/agent_runtime.rs` implements `ConversationService` over the composed `claw-runtime` runtime. Nothing replays `compat/legacy` against it. |
| `src/channels/telegramPolling.ts` | Offset-based long polling, retry/stop behavior, inbound text normalization, engine dispatch, and segmented replies. | `claw-channels` | **Composed — evidence outstanding.** `claw-channels::telegram` still reports `CompatibilityShim` and owns offset-based long polling, the frozen poll and send timeouts, retry/stop behavior, inbound normalization, and segmented replies. `TelegramHttpTransport` in the daemon implements `TelegramTransport` over the shared `HttpTransport` and origin-trust check, and the supervisor drives it on the configured poll interval. Nothing replays `compat/legacy` against it. |
| `src/channels/whatsappWebhook.ts` | Webhook challenge verification, inbound payload parsing, engine dispatch, Graph API replies, and segmented output. | `claw-channels` + `claw-http-api` | **Composed — evidence outstanding.** `claw-channels::whatsapp` still reports `CompatibilityShim` and owns `hub.*` challenge verification, payload normalization, Graph v20 replies, and segmented output; `claw-http-api` registers `GET`/`POST /whatsapp/webhook` behind `LegacyWhatsAppServices`. `GraphWhatsAppTransport` implements `WhatsAppTransport` and `GraphWhatsAppAdapter` implements `LegacyWhatsAppPort`, both in the daemon, and the routes are registered only when WhatsApp credentials resolve. Nothing replays `compat/legacy` against it. |
| `src/config.ts` | Exact environment parsing, defaults, bounds, URL/domain normalization, authentication mode, and conditional channel credential validation. | `claw-config` | **Composed — evidence outstanding.** `ProductionOptions::load_config` is the startup path: with a configuration file it resolves `ConfigLayers` over the built-in, file, and process-environment layers; with no file it runs `migrate_legacy_environment` across the whole process environment and labels the source `legacy-environment`. Applied, manual-required, and ignored-unknown diagnostics are counted, recorded, and logged, and conditional channel credentials are validated in `legacy_settings` before any channel starts. `--check-config` validates without serving. Nothing replays `compat/legacy` against it, and Windows durability after sudden power loss is still not claimed. |
| `src/engine/copilotEngine.ts` | Provider startup/ping, per-conversation sessions, system prompt/model/tools, approval policy, timeouts, fixed fallback text, reload fencing, and shutdown. | `claw-runtime` + `claw-provider-sdk` + `claw-tools` + `claw-state` (**not in this repository**) | **Partial — implementation and composition.** Composed and real: `claw-runtime::Runtime` runs in `apps/gta-claw-daemon/src/adapters/agent_runtime.rs` with a `claw-memory` context engine, a durable `claw-goals::FileGoalStore` under the state directory, the role prompt and configured model, the request timeout read from typed configuration, a startup ping and model-catalogue check (`ProviderAdapter::initialize`, which refuses a configured model the live catalogue does not list), token-driven engine swap through `SwappableProvider`, reload fencing under a shared lock, and an accounted drain. Still missing: `claw-state` does not exist here, so `RuntimeStateStore` keeps sessions and turns in a process `Mutex<HashMap>` and a restart loses all of them; the approval policy is `claw_runtime::approval::SilentApprovalPort`, which approves every request without asking anyone; and `claw-tools` is a dependency of no binary, so the tool surface the model sees is the signed plugin registrations plus the durable goal tool, not `claw-tools`. |
| `src/engine/sessionManager.ts` | Touch-on-read, capacity LRU eviction, strict idle TTL cleanup, clear, and terminal destruction. | `claw-runtime` + `claw-state` (**not in this repository**) | **Partial — implementation and composition.** `ProviderAdapter` in the daemon implements touch-on-read, oldest-first capacity eviction, and idle-TTL expiry over `ProviderHistoryConfig`, whose bounds come from typed configuration, and `clear_history` on logout; `RuntimeConfig::session_idle_ttl` carries the same bound into the runtime. Two things remain. Nothing retains session content across a restart, because the persistent-cleanup owner still does not exist in the tree. And TTL expiry has no acceptance coverage: `crates/claw-http-api/tests/legacy_http.rs` asserts the capacity bound and the clear, and no daemon test asserts the TTL. |
| `src/engine/toolExecutor.ts` | Tool registry, domain policy, bounded HTTP/log bridges, disposal, and JavaScript evaluation. | `claw-tools` + `claw-skills` | **Partial by deliberate break — composition.** JavaScript evaluation must be removed, not ported, and it has been. What is composed is a different registry from the one this row assigns: signed Wasm plugin tools published through `claw-plugin-host` under an Ed25519 trust policy, a deny-all service base extended only with logs, config, store, pinned HTTP, bounded DNS, clock, random, tools, and discarded events, plus the durable goal tool. `claw-tools` is depended on by no binary, so its closed schemas, deny-by-default authorization, path confinement, and validated network destinations are not in any running service. `claw-skills` execution is likewise not dispatched: the daemon reads `claw_skills::registry().len()` for its inventory count, and the `PluginWasmSkillHost` bridge that implements `WasmSkillHost` has no caller on the production path. |
| `src/index.ts` | Full service composition, startup ordering, proxy/config/role/skill loading, token-driven engine swaps, channel lifecycle, reload transaction, update check, and graceful shutdown. | `gta-claw-daemon` composition over all replacement crates | **Composed — evidence outstanding, with named gaps.** `apps/gta-claw-daemon` depends on 20 `claw-*` crates and `gta-claw-updater`. `production.rs` resolves configuration and recovery guidance, installs the redacting subscriber, derives one shared proxy policy, loads the role document over it, activates signed plugins under a bounded candidate limit and deadline, brings the provider up or leaves it pending Device Flow, starts the channel supervisor, opens a durable audit log and a durable Gateway pairing store, binds a `claw-gateway` server plus three `TcpListener`s (main HTTP, legacy HTTP, loopback MCP), fires one signed update check, serves a transactional reload, and drains all of it into an accounted stop summary. What it does **not** reach: durable session and turn state, `claw-tools`, skill-execution dispatch, an interactive approval surface, watch pairing and task-flow webhooks (wired to `DisabledExternalPorts`, which fails closed), and plugin/skill HTTP, which uses `claw-plugin-host`'s `PinnedHttpTransport` rather than the shared proxy policy. No test in `apps/` reads `compat/legacy`, so the composition is unproven against the frozen contract — which is why every row above is evidence rather than done. |
| `src/loader/roleLoader.ts` | Bounded remote HTTP fetch, JSON/plain-text fallback, `content`/`prompt` precedence, optional model handling, and diagnostics. | `claw-config` | **Composed — evidence outstanding.** `claw-config::role` owns the interpretation half: the 1 MiB bound, the frozen `Accept` header and timeout, status and declared-length checks, `content`/`prompt` precedence, plain-text fallback, optional model, and returned diagnostics. The transport half is no longer a test double only: `TransportRoleFetcher` in `apps/gta-claw-daemon/src/production.rs` implements `RoleSourceFetcher` over `claw-provider-sdk`'s `HttpTransport` under the shared proxy policy, `load_role` runs on the startup path before readiness and is cancellable by a stop signal, and every returned diagnostic is recorded and logged. Nothing replays `compat/legacy` against it. |
| `src/loader/skillLoader.ts` | Concurrent bounded remote fetch, per-skill validation, safe names, partial success, and input-order retention. | `claw-skills`, plus an unassigned fetch owner | **Partial — owner required for the fetch half.** Legacy input is migration data only; signed Rust/WASI discovery and equivalent diagnostics must replace executable loading. `claw-skills` discovers and validates the bundled registry and classifies legacy manifests, and the daemon now performs *local* signed plugin discovery through `claw-plugin-host` with a candidate limit, an activation deadline, cancellation, and a stable discovery-ordered activation report. Nothing in this tree owns the concurrent bounded **remote** fetch, its partial success, or its input-order retention, so this row's missing piece is still an owner. |
| `src/server.ts` | Rate limiting plus `/`, `/health`, `/auth/device`, `/chat`, `/api/messages`, WhatsApp webhook, admin reload, system, and allowlisted exec response contracts. | `claw-http-api` | **Composed — evidence outstanding.** `claw-http-api::legacy` registers all ten legacy method/path identities (`LEGACY_HTTP_ENDPOINTS`), the rate limiter, the ordered `/admin/exec` allowlist, and the conditional Teams and WhatsApp routes, with frozen-shape acceptance tests in `crates/claw-http-api/tests/legacy_http.rs`. The daemon binds that router to a real `TcpListener` — the configured `core.server.port` on loopback unless `--legacy-listen` overrides it — behind in-flight request accounting and a graceful-shutdown token, and a routable bind is refused unless `--tls-terminated-by-frontend` is passed. Every `LegacyApiServices` slot now has a concrete daemon adapter: `AgentRuntime` for the runtime and channel-message ports, `LegacyDeviceFlowAdapter`, `LegacyTeamsAdapter`, `GraphWhatsAppAdapter`, `DaemonLegacyReload`, and `NativeLegacyHostAdmin` (installed only when a bearer credential is configured; without one the protected routes are disabled and the daemon warns). `ProviderLegacyRuntime` is no longer used by any binary. Nothing replays `compat/legacy` against the bound service. |
| `src/updater/sdkUpdater.ts` | Nonblocking installed/latest version reporting and optional package/CLI mutation. | `gta-claw-updater` | **Composed when enabled — evidence outstanding.** Package-manager and curl self-mutation are deliberately forbidden; only verified signed releases may replace them. `apps/gta-claw-updater` is that owner, and `apps/gta-claw-daemon/src/adapters/updater.rs::UpdateMonitor` now performs the legacy nonblocking startup check: one supervised background `Updater::check` against a signature-verified manifest, joined within a budget at shutdown and aborted at the deadline. It runs only when `core.updates.enabled` is set, and it then requires `GTA_CLAW_UPDATE_MANIFEST` and `GTA_CLAW_UPDATE_TARGET` and refuses to start under an explicit proxy policy, so a default deployment performs no check at all. On Linux the updater still defers to the system package manager by design. |
| `src/utils/logger.ts` | Structured level-controlled logging used by every runtime component. | `claw-observability` | **Composed — evidence outstanding.** Rust logs need not reproduce pino bytes. `claw-observability` owns the pipeline and re-exports the `tracing` facade so no caller opens a second logging path, and `RedactingLayer` redacts field values. `gta-claw-daemon` now depends on the crate and `init_telemetry` installs the redacting subscriber unconditionally on the serve path, at the level from `core.logging.level`, overridable by `GTA_CLAW_LOG`, in human or JSON form, to standard error or `--log-file`; every startup stage, the drain, and each subsystem fault log through it, and the handle is shut down and checked for late writer failures before the process exits. `gta-claw-cli` and `gta-claw-tui` still install it only when verbosity is raised explicitly. Lifecycle and error-field acceptance coverage against the frozen contract is still outstanding. |
| `src/utils/proxy.ts` | `HTTPS_PROXY`/`HTTP_PROXY` precedence, global outbound dispatcher setup, redacted diagnostics, and continue-without-proxy failure behavior. | `claw-provider-sdk` | **Partial — composition.** `claw-provider-sdk::http::proxy` owns the policy: the legacy `HTTPS_PROXY`/`https_proxy`/`HTTP_PROXY`/`http_proxy`/`ALL_PROXY`/`all_proxy` precedence, `NO_PROXY` bypass matching, diagnostics that never echo the URL, and continue-without-proxy on a malformed one. The daemon derives one `ProxyPolicy` from configuration, records its diagnostics, warns when it falls back to direct, and shares that single value with the provider, the role fetch, the Teams and WhatsApp transports, and the Telegram and Discord transports. One outbound path is still outside it: plugin and skill HTTP goes through `claw-plugin-host`'s `PinnedHttpTransport`, which has no proxy support, so the reviewed policy is shared by most of the running service but not all of it. |
| `src/utils/splitMessage.ts` | Newline/word-aware bounded splitting with hard slicing fallback. | `claw-channel-sdk` + `claw-channels` | **Composed for the four proven channels — evidence outstanding, and 25 channels still without a proven limit.** `claw-channel-sdk::segmentation` implements the newline-then-space-then-hard-cut preference over an explicit counting unit, and the daemon's live Telegram and Discord senders call `segment_outbound_text_iter` on every reply. Four channels carry a limit proven by `compat/legacy/ledger/behaviors.json` — Teams and Telegram 4,000, Discord 1,900, WhatsApp 3,500, all in UTF-16 code units, the unit the legacy `String.length` counted — and segment against it. The other 25 have no stated limit anywhere in the tree, so segmentation refuses with `SegmentationError::NoDeclaredLimit` rather than cutting against an invented bound, and no composed transport exists for them either. |

## Build and dependency obligations

| Legacy path or dependency | Current role | Removal condition |
|---|---|---|
| `Dockerfile` | The image built by `.github/workflows/docker-publish.yml`; compiles and runs `dist/index.js`. | Replace only after the native daemon closes the gaps named in the `src/index.ts` row and container acceptance tests cover the legacy service contract. The daemon now composes and binds the service, so this condition is no longer "wait for a composition" but "wait for the evidence and the named gaps". |
| `package.json` | Defines the TypeScript build/start path and production package graph. | Delete together with the final Node runtime, never before it. |
| `package-lock.json` | Pins the published container's production dependency graph. | Delete with `package.json` after the native image cutover. |
| `tsconfig.json` | Produces `dist/` from the legacy TypeScript service. | Delete with the TypeScript source after the native image cutover. |
| `dist/` | Generated build output; currently untracked. | Remains ignored and is not allowed in the repository ratchet. |
| `@github/copilot-sdk` | Production model/session dependency of the legacy Node service. | Replaced by `claw-providers::github_copilot`, which speaks the device grant and chat surface over `claw-provider-sdk`'s pure-Rust HTTPS stack. `gta-claw-daemon` now uses that client, so the remaining condition is the image cutover rather than the composition. It remains forbidden from the Rust product dependency graph. |
| `botbuilder`, `restify`, `undici`, `ws`, `pino` | Production channel, HTTP, network, WebSocket, and logging dependencies. | Remove only when their module-level obligations above have composed Rust replacements **and** those replacements are proven against `compat/legacy`. Each replacement — `claw-channels`, `claw-http-api`, `claw-provider-sdk`, `claw-channels::discord`, and `claw-observability` respectively — is now composed in `gta-claw-daemon`, so every one of these is an evidence-and-cutover condition, not an ownership or composition one. |
| `isolated-vm` and `node:vm` | Execute remotely supplied JavaScript skills. | Deliberate removal; never add an embedded JavaScript engine to Rust. |

## Final deletion checklist

1. Land the Rust provider, runtime, state, tools, channels, HTTP, config, and shared network
   adapters with the exact dependency pins required by repository policy. Most of these owners have
   landed and are tested, and the ones this step used to list as outstanding — an implementation of
   `RoleSourceFetcher`, the channel transport ports (`TelegramTransport`, `DiscordTransport`,
   `WhatsAppTransport`), Teams JWT verification, and the concrete `LegacyApiServices` adapters other
   than `ProviderLegacyRuntime` — now exist in `apps/gta-claw-daemon`. What is still outstanding here
   is `claw-state`, an owner for the concurrent bounded remote skill fetch, and adoption of the
   shared proxy policy by plugin and skill HTTP.
2. Compose those adapters in `gta-claw-daemon`; no mock, metadata-only, registry-only, or
   stand-in adapter satisfies a production obligation, and no crate discharges an obligation by
   implementing it. This step has largely happened: `main.rs` runs `production.rs`, which binds the
   Gateway, main HTTP, legacy HTTP, and loopback MCP listeners over the shipped crates. It is not
   finished — durable session state, `claw-tools`, skill-execution dispatch, an interactive approval
   surface, and the watch and task-flow ports are still uncomposed, and `--smoke` exists precisely
   because a local diagnostic provider must never be mistaken for one of them.
3. Pass every behavior and HTTP shape in `compat/legacy`, including negative, timeout, reload-race,
   channel, persistence, and shutdown cases. **This is now the active step, and nothing has been
   done on it:** no test under `apps/` reads `compat/legacy`, which is why every composed row above
   still says "evidence outstanding".
4. Port any remaining JavaScript tests to Rust. Do not widen the ratchet for test files or generated
   `dist/` output.
5. Switch `Dockerfile` and `docker-publish.yml` to the native service and validate the built image,
   health behavior, non-root runtime, shutdown, and all enabled channel paths.
6. Delete legacy files and their corresponding `LEGACY_RUNTIME_INVENTORY` entries in the same
   change. The allowlist must monotonically approach empty.
