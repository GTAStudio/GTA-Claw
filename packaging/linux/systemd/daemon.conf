# Credential-file placeholder.
#
# systemd exposes this file as $CREDENTIALS_DIRECTORY/gta-claw-config. The
# current daemon does not consume it; it exists to make the future secret
# injection boundary explicit without placing secret literals in environment
# variables or unit files.
