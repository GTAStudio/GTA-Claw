// Accepted fixture for pinned top-level domain `discovery`
// (compat/upstream/inventories/config-domains.json).
{
  discovery: {
    wideArea: { enabled: true, domain: "claw.example.test" },
    mdns: { mode: "minimal" },
  },
}
