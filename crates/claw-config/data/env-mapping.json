{
  "$schema": "../schemas/config-mapping.schema.json",
  "source_revision": "b2896426f3fcc5bb149402a38e09aac5e836d70b",
  "proposed_format": "JSON5",
  "mappings": [
    {
      "legacy_env": "GITHUB_TOKEN",
      "scope": "runtime",
      "target_json5_key": "auth.github.pat",
      "secret": true,
      "default": null,
      "conversion": "Trim; empty becomes absent. Store as a secret reference rather than inline JSON5.",
      "validation": "Optional only when auth.github.device.enabled is true with a client_id.",
      "required_when": "auth.github.device.enabled is false"
    },
    {
      "legacy_env": "DEVICE_FLOW_ENABLED",
      "scope": "runtime",
      "target_json5_key": "auth.github.device.enabled",
      "secret": false,
      "default": "true when GITHUB_CLIENT_ID is nonempty, otherwise false",
      "conversion": "Accept only exact lowercase true or false.",
      "validation": "When true, auth.github.device.client_id is required.",
      "required_when": ""
    },
    {
      "legacy_env": "GITHUB_CLIENT_ID",
      "scope": "runtime",
      "target_json5_key": "auth.github.device.client_id",
      "secret": false,
      "default": null,
      "conversion": "Trim; empty becomes absent.",
      "validation": "Nonempty string when device flow is enabled.",
      "required_when": "auth.github.device.enabled is true"
    },
    {
      "legacy_env": "MicrosoftAppId",
      "scope": "runtime",
      "target_json5_key": "channels.teams.app_id",
      "secret": false,
      "default": null,
      "conversion": "Trim; empty becomes absent.",
      "validation": "Nonempty when Teams is enabled.",
      "required_when": "channels.teams.enabled is true"
    },
    {
      "legacy_env": "MicrosoftAppPassword",
      "scope": "runtime",
      "target_json5_key": "channels.teams.app_password",
      "secret": true,
      "default": null,
      "conversion": "Trim; empty becomes absent. Store as a secret reference.",
      "validation": "Nonempty when Teams is enabled.",
      "required_when": "channels.teams.enabled is true"
    },
    {
      "legacy_env": "AGENT_ROLE_URL",
      "scope": "runtime",
      "target_json5_key": "role.source_url",
      "secret": false,
      "default": null,
      "conversion": "Copy without trimming after required-value lookup.",
      "validation": "Required absolute http or https URL.",
      "required_when": "always"
    },
    {
      "legacy_env": "ENABLED_SKILLS",
      "scope": "runtime",
      "target_json5_key": "legacy.skills.source_urls",
      "secret": false,
      "default": [],
      "conversion": "Split on commas, trim entries, drop empty entries, validate each URL.",
      "validation": "Every nonempty entry is an absolute http or https URL.",
      "required_when": ""
    },
    {
      "legacy_env": "ENABLE_TEAMS",
      "scope": "runtime",
      "target_json5_key": "channels.teams.enabled",
      "secret": false,
      "default": true,
      "conversion": "Accept only exact lowercase true or false.",
      "validation": "Requires Teams app ID and password when true.",
      "required_when": ""
    },
    {
      "legacy_env": "ENABLE_TELEGRAM",
      "scope": "runtime",
      "target_json5_key": "channels.telegram.enabled",
      "secret": false,
      "default": false,
      "conversion": "Accept only exact lowercase true or false.",
      "validation": "Requires bot_token when true.",
      "required_when": ""
    },
    {
      "legacy_env": "TELEGRAM_BOT_TOKEN",
      "scope": "runtime",
      "target_json5_key": "channels.telegram.bot_token",
      "secret": true,
      "default": null,
      "conversion": "Trim; empty becomes absent. Store as a secret reference.",
      "validation": "Nonempty when Telegram is enabled.",
      "required_when": "channels.telegram.enabled is true"
    },
    {
      "legacy_env": "TELEGRAM_POLL_INTERVAL_MS",
      "scope": "runtime",
      "target_json5_key": "channels.telegram.poll_interval_ms",
      "secret": false,
      "default": 2000,
      "conversion": "Parse the leading base-10 integer, matching Number.parseInt.",
      "validation": "Finite integer prefix from 500 through 60000.",
      "required_when": "",
      "known_legacy_quirk": "Values such as 2000ms are accepted as 2000 by the legacy parser."
    },
    {
      "legacy_env": "ENABLE_DISCORD",
      "scope": "runtime",
      "target_json5_key": "channels.discord.enabled",
      "secret": false,
      "default": false,
      "conversion": "Accept only exact lowercase true or false.",
      "validation": "Requires bot_token when true.",
      "required_when": ""
    },
    {
      "legacy_env": "DISCORD_BOT_TOKEN",
      "scope": "runtime",
      "target_json5_key": "channels.discord.bot_token",
      "secret": true,
      "default": null,
      "conversion": "Trim; empty becomes absent. Store as a secret reference.",
      "validation": "Nonempty when Discord is enabled.",
      "required_when": "channels.discord.enabled is true"
    },
    {
      "legacy_env": "DISCORD_GATEWAY_URL",
      "scope": "runtime",
      "target_json5_key": "channels.discord.gateway_url",
      "secret": false,
      "default": "wss://gateway.discord.gg/?v=10&encoding=json",
      "conversion": "Trim; use default when empty.",
      "validation": "No legacy URL validation is applied.",
      "required_when": ""
    },
    {
      "legacy_env": "DISCORD_GATEWAY_INTENTS",
      "scope": "runtime",
      "target_json5_key": "channels.discord.gateway_intents",
      "secret": false,
      "default": 33281,
      "conversion": "Parse the leading base-10 integer, matching Number.parseInt.",
      "validation": "Finite integer prefix greater than or equal to 1.",
      "required_when": ""
    },
    {
      "legacy_env": "ENABLE_WHATSAPP",
      "scope": "runtime",
      "target_json5_key": "channels.whatsapp.enabled",
      "secret": false,
      "default": false,
      "conversion": "Accept only exact lowercase true or false.",
      "validation": "Requires verify_token, access_token, and phone_number_id when true.",
      "required_when": ""
    },
    {
      "legacy_env": "WHATSAPP_VERIFY_TOKEN",
      "scope": "runtime",
      "target_json5_key": "channels.whatsapp.verify_token",
      "secret": true,
      "default": null,
      "conversion": "Trim; empty becomes absent. Store as a secret reference.",
      "validation": "Nonempty when WhatsApp is enabled.",
      "required_when": "channels.whatsapp.enabled is true"
    },
    {
      "legacy_env": "WHATSAPP_ACCESS_TOKEN",
      "scope": "runtime",
      "target_json5_key": "channels.whatsapp.access_token",
      "secret": true,
      "default": null,
      "conversion": "Trim; empty becomes absent. Store as a secret reference.",
      "validation": "Nonempty when WhatsApp is enabled.",
      "required_when": "channels.whatsapp.enabled is true"
    },
    {
      "legacy_env": "WHATSAPP_PHONE_NUMBER_ID",
      "scope": "runtime",
      "target_json5_key": "channels.whatsapp.phone_number_id",
      "secret": false,
      "default": null,
      "conversion": "Trim; empty becomes absent.",
      "validation": "Nonempty when WhatsApp is enabled.",
      "required_when": "channels.whatsapp.enabled is true"
    },
    {
      "legacy_env": "WHATSAPP_WEBHOOK_PATH",
      "scope": "runtime",
      "target_json5_key": "channels.whatsapp.webhook_path",
      "secret": false,
      "default": "/whatsapp/webhook",
      "conversion": "Trim; use default when empty.",
      "validation": "Must start with slash.",
      "required_when": ""
    },
    {
      "legacy_env": "PORT",
      "scope": "runtime",
      "target_json5_key": "server.port",
      "secret": false,
      "default": 3978,
      "conversion": "Parse the leading base-10 integer, matching Number.parseInt.",
      "validation": "Finite integer prefix from 1 through 65535.",
      "required_when": ""
    },
    {
      "legacy_env": "LOG_LEVEL",
      "scope": "runtime",
      "target_json5_key": "logging.level",
      "secret": false,
      "default": "info",
      "conversion": "Copy exactly without case normalization.",
      "validation": "One of trace, debug, info, warn, error, fatal.",
      "required_when": ""
    },
    {
      "legacy_env": "NODE_ENV",
      "scope": "runtime",
      "target_json5_key": "logging.development_transport",
      "secret": false,
      "default": false,
      "conversion": "Set true only when the value is exactly development.",
      "validation": "No other values are rejected.",
      "required_when": ""
    },
    {
      "legacy_env": "SESSION_TTL_MS",
      "scope": "runtime",
      "target_json5_key": "sessions.ttl_ms",
      "secret": false,
      "default": 3600000,
      "conversion": "Parse the leading base-10 integer, matching Number.parseInt.",
      "validation": "Finite integer prefix greater than or equal to 1000.",
      "required_when": ""
    },
    {
      "legacy_env": "MAX_SESSIONS",
      "scope": "runtime",
      "target_json5_key": "sessions.max_entries",
      "secret": false,
      "default": 100,
      "conversion": "Parse the leading base-10 integer, matching Number.parseInt.",
      "validation": "Finite integer prefix greater than or equal to 1.",
      "required_when": ""
    },
    {
      "legacy_env": "COPILOT_MODEL",
      "scope": "runtime",
      "target_json5_key": "copilot.default_model",
      "secret": false,
      "default": "gpt-4o",
      "conversion": "Copy exactly; a role model overrides it.",
      "validation": "No legacy allow-list validation is applied.",
      "required_when": ""
    },
    {
      "legacy_env": "SKILL_EXEC_TIMEOUT_MS",
      "scope": "runtime",
      "target_json5_key": "legacy.skills.execution_timeout_ms",
      "secret": false,
      "default": 30000,
      "conversion": "Parse the leading base-10 integer, matching Number.parseInt.",
      "validation": "Finite integer prefix greater than or equal to 100.",
      "required_when": ""
    },
    {
      "legacy_env": "SDK_REQUEST_TIMEOUT_MS",
      "scope": "runtime",
      "target_json5_key": "copilot.request_timeout_ms",
      "secret": false,
      "default": 120000,
      "conversion": "Parse the leading base-10 integer, matching Number.parseInt.",
      "validation": "Finite integer prefix greater than or equal to 1000.",
      "required_when": ""
    },
    {
      "legacy_env": "RATE_LIMIT_PER_MIN",
      "scope": "runtime",
      "target_json5_key": "server.teams_rate_limit_per_minute",
      "secret": false,
      "default": 30,
      "conversion": "Parse the leading base-10 integer, matching Number.parseInt.",
      "validation": "Finite integer prefix greater than or equal to 1.",
      "required_when": ""
    },
    {
      "legacy_env": "ALLOWED_SKILL_DOMAINS",
      "scope": "runtime",
      "target_json5_key": "legacy.skills.allowed_domains",
      "secret": false,
      "default": [],
      "conversion": "Split on commas, trim, lowercase, remove empty values, and deduplicate while preserving first occurrence order.",
      "validation": "No hostname syntax validation; an empty list allows every domain.",
      "required_when": ""
    },
    {
      "legacy_env": "DOMAIN",
      "scope": "runtime",
      "target_json5_key": "server.public_domain",
      "secret": false,
      "default": "localhost",
      "conversion": "Copy exactly without trimming.",
      "validation": "No legacy validation is applied.",
      "required_when": ""
    },
    {
      "legacy_env": "AUTO_UPDATE",
      "scope": "runtime",
      "target_json5_key": "updates.enabled",
      "secret": false,
      "default": false,
      "conversion": "Accept only exact lowercase true or false.",
      "validation": "Boolean; true is rejected because dependency updates are review-only.",
      "required_when": "",
      "known_legacy_quirk": "The compatibility runtime preserves read-only update checks; AUTO_UPDATE=true fails instead of invoking npm or curl."
    },
    {
      "legacy_env": "ADMIN_TOKEN",
      "scope": "runtime",
      "target_json5_key": "admin.bearer_token",
      "secret": true,
      "default": null,
      "conversion": "Copy exactly without trimming. Empty disables all admin route registration.",
      "validation": "Any nonempty string is accepted.",
      "required_when": ""
    },
    {
      "legacy_env": "TRUST_PROXY",
      "scope": "runtime",
      "target_json5_key": "server.trust_proxy",
      "secret": false,
      "default": false,
      "conversion": "Accept only exact lowercase true or false.",
      "validation": "Boolean.",
      "required_when": ""
    },
    {
      "legacy_env": "HTTPS_PROXY",
      "aliases": ["https_proxy", "HTTP_PROXY", "http_proxy", "ALL_PROXY", "all_proxy"],
      "scope": "runtime",
      "target_json5_key": "network.proxy_url",
      "secret": true,
      "default": null,
      "conversion": "Use the first nonempty value in alias order. Redact userinfo in successful log output.",
      "validation": "ProxyAgent parses the URL; parse failure is logged and startup continues without the proxy.",
      "required_when": ""
    },
    {
      "legacy_env": "COPILOT_CLI_PATH",
      "scope": "runtime",
      "target_json5_key": "copilot.cli_path",
      "secret": false,
      "default": "copilot.cmd on Windows, otherwise copilot",
      "conversion": "Copy path exactly.",
      "validation": "Execution failure yields installed version unknown.",
      "required_when": ""
    },
    {
      "legacy_env": "DOCKER_IMAGE",
      "scope": "deployer",
      "target_json5_key": "deployment.image",
      "secret": false,
      "default": "aizhihuxiao/gta-claw:latest",
      "conversion": "Copy the image reference.",
      "validation": "run.sh requires a lowercase repository path with optional registry port and tag.",
      "required_when": ""
    },
    {
      "legacy_env": "APP_LANG",
      "scope": "deployer",
      "target_json5_key": "deployment.ui_language",
      "secret": false,
      "default": "zh",
      "conversion": "en, EN, english, or English map to en; every other nonempty value maps to zh.",
      "validation": "No rejection.",
      "required_when": ""
    },
    {
      "legacy_env": "COPILOT_CLI_VERSION",
      "scope": "build",
      "target_json5_key": "build.copilot_cli_version",
      "secret": false,
      "default": null,
      "conversion": "Pass to the official installer as VERSION when nonempty.",
      "validation": "Installer-defined.",
      "required_when": ""
    },
    {
      "legacy_env": "DOCKERHUB_USERNAME",
      "scope": "ci",
      "target_json5_key": "publish.dockerhub.username",
      "secret": true,
      "default": null,
      "conversion": "Lowercase when deriving the default image namespace.",
      "validation": "Required by the publish workflow.",
      "required_when": "publishing"
    },
    {
      "legacy_env": "DOCKERHUB_TOKEN",
      "scope": "ci",
      "target_json5_key": "publish.dockerhub.token",
      "secret": true,
      "default": null,
      "conversion": "Use as Docker Hub login token.",
      "validation": "Required by the publish workflow.",
      "required_when": "publishing"
    },
    {
      "legacy_env": "DOCKERHUB_IMAGE",
      "scope": "ci",
      "target_json5_key": "publish.dockerhub.image",
      "secret": false,
      "default": "docker.io/<lowercase DOCKERHUB_USERNAME>/gta-claw",
      "conversion": "Lowercase the complete value.",
      "validation": "Must match docker.io/<lowercase namespace>/<lowercase repo> with no tag.",
      "required_when": ""
    }
  ]
}
